  pipeline {
    agent {
      label 'master'
    }  
    
    stages {
     stage('build') {
         steps {
            // build
                 sh 'ant -f build.xml -v'
          
         }    
     }
     stage('deploy') {
         steps {
             echo 'Deploying...' 
         }
     }
    }
  }
