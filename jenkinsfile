  pipeline {
    agent {
      label 'master'
    }  
    
    stages {
     stage('build') {
         steps {
            // build
                 sh 'ant -f build.xml -v'
          
         }    
      }
     
      stage('deploy') {
         steps {
            // deploy

         }
      }
   }
    
   
    post {
      always {
        archiveArtifacts artifacts: 'dist/*.jar', fingerprint: true
      } 
    }
  }
